{
  "name": "fluxroute-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "prisma generate && tsc",
    "dev": "tsx watch src/main.ts",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "start": "node dist/main.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@prisma/client": "^5.22.0",
    "fastify": "^5.8.5",
    "node-cron": "^3.0.3",
    "prisma": "^5.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.6",
    "@types/node-cron": "^3.0.11",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^4.0.15"
  }
}
